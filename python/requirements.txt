# Pinned runtime for Colab T4 / local CPU (matches python.yml CI + notebook).
# Local quick gate (`make smoke`, `make types`) needs NONE of these.
torch>=2.4.1
numpy>=1.26
pandas>=2.1
loguru>=0.7
matplotlib>=3.8
scipy>=1.11
onnx>=1.16
onnxscript>=0.1      # torch>=2.13 ONNX exporter needs this
tensorboard>=2.16
scikit-learn>=1.3
